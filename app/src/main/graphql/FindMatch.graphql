query FindMatch($input:InputFilter!) {
    matches(input: $input) {
        displayName
        age
        jobTitle
        distance
        mainPhoto
        contactsExchanged
        city {
            name
        }
        favourite
        heightInCm
        compatibilityScore
    }
}